# Show Swagger UI at root
springdoc.swagger-ui.path=/

# Swagger JSON still accessible (optional)
springdoc.api-docs.path=/api/v1/api-docs

# Optional: set title in Swagger UI tab
springdoc.swagger-ui.title=GetWarranty API Docs
